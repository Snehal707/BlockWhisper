'use client';

// Similar to FarcasterClient but tailored for Base/Coinbase Wallet integration
// Initially can be a copy of the Web client or Farcaster client depending on requirements.
// For now, let's keep it minimal or clone Web client behavior.

export default function BaseClient() {
    return (
        <div className="flex flex-col items-center justify-center min-h-screen text-white">
            <h1>Base Mini App</h1>
            <p>Coming soon...</p>
        </div>
    );
}
